CREATE TABLE users (id SERIAL PRIMARY KEY, username VARCHAR(80) UNIQUE NOT NULL, password VARCHAR(80) NOT NULL, credits INTEGER NOT NULL default 10, wins INTEGER NOT NULL default 0, loses INTEGER NOT NULL default 0, ties INTEGER NOT NULL default 0);